[package]
name = "wrtctrl-jni"
version = "0.1.0"
edition = "2021"
description = "WrtCtrl JNI 导出层（cdylib，仅 Android 目标编译）"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
wrtctrl-core = { path = "../wrtctrl-core" }
