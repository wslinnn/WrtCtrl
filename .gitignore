*.iml
.gradle/
/local.properties
.idea/
.DS_Store
build/
/captures
.externalNativeBuild
.cxx

# Rust 构建产物（workspace 级 target/，由 gradle buildRustLibs 任务经 cargo-ndk 生成）
app/src/main/jniLibs/
/target/

# 签名文件不入库（本地：release.keystore + keystore.properties；CI 由 secrets 环境变量注入）
release.keystore
keystore.properties
*.jks

# 本地开发文档不入库
/DEVPLAN.md
/AGENTS.md
/docs/research/
/docs/review/
/docs/specs/

# Mimosa 安全扫描插件的工作区状态（hook 状态等本地产物，不入库）
.mimosa/
